{
  "name": "panoptes-network",
  "version": "0.0.5",
  "private": true,
  "scripts": {
    "install": "cd data-explorer && npm install . && cd ..",
    "emulate": "cd functions && npm run serve && cd ..",
    "serve": "python resources/populate-fs-emulator.py resources/firestore.emulator.data.json && cd data-explorer && npm run serve && cd ..",
    "build": "cd data-explorer && npm run build && cd ..",
    "test:unit": "cd data-explorer && npm run test:unit && cd .."
  },
  "dependencies": {
    "@vue/cli-service": "^4.1.2"
  },
  "devDependencies": {}
}
